
1. Install zod using `pnpm i zod`  

2. Now we will create a new folder named `validation` and in that we will create new file named `request.validation.js`

3. Under that we will create and export a new validation schema which will check the user provided body in request using zod expectations
```js
// Importing z to create a schema for user validation
import { email, z } from 'zod'


// exporting user validation schema
export const signupPostRequestBodySchema = z.object({

    // Expectations from the request body

    firstname: z.string(),

    lastname: z.string().optional(),

    email: z.string().email(),

    password: z.string().min(3),

})
```

4. Importing validation in `user.routes.js` file 
```js

```